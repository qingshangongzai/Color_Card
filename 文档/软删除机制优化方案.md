# 软删除机制优化方案

## 目标

将软删除机制完全转移到业务层（`ConfigManager`），UI层只负责UI更新，不再直接操作数据。

## 当前问题

1. **职责边界不清晰**：UI层直接修改数据（`_deleted` 标记）
2. **重复操作**：`_deleted` 标记被设置两次
3. **索引管理分散**：`_valid_indices` 在 UI 层维护

## 优化方案

### 一、业务层增强（core/config.py）

在 `ConfigManager` 类中添加以下方法：

```python
def get_valid_indices(self) -> Set[int]:
    """获取有效（未删除）数据的索引集合
    
    Returns:
        Set[int]: 有效数据的索引集合
    """
    favorites = self._config.get('favorites', [])
    return {i for i, f in enumerate(favorites) if not f.get('_deleted')}

def get_favorite_index(self, favorite_id: str) -> int:
    """获取指定收藏在原始列表中的索引
    
    Args:
        favorite_id: 收藏ID
        
    Returns:
        int: 索引位置，未找到返回 -1
    """
    favorites = self._config.get('favorites', [])
    for i, f in enumerate(favorites):
        if f.get('id') == favorite_id:
            return i
    return -1
```

### 二、UI层简化（ui/palette_management.py）

#### 1. 修改 `set_favorites` 方法

**当前代码（第899-912行）：**
```python
def set_favorites(self, favorites):
    """设置收藏列表"""
    self._favorites = favorites
    # 预计算有效（未删除）数据的索引集合，提高查找性能
    self._valid_indices = {i for i, f in enumerate(favorites) if not f.get('_deleted')}
    self._groups = generate_groups(len(favorites))
    ...
```

**优化后：**
```python
def set_favorites(self, favorites):
    """设置收藏列表"""
    self._favorites = favorites
    # 从业务层获取有效索引，而不是自己计算
    self._valid_indices = self._config_manager.get_valid_indices()
    self._groups = generate_groups(len(favorites))
    ...
```

#### 2. 修改 `remove_favorite_card` 方法

**当前代码（第1159-1182行）：**
```python
def remove_favorite_card(self, favorite_id: str) -> bool:
    """标记删除指定收藏卡片（软删除）"""
    card = self._favorite_cards.get(favorite_id)
    if not card:
        return False

    # 从UI移除卡片
    self.content_layout.removeWidget(card)
    card.deleteLater()
    del self._favorite_cards[favorite_id]

    # 标记数据为已删除（不从 _favorites 中移除，保持索引不变）
    for idx, fav in enumerate(self._favorites):
        if fav.get('id') == favorite_id:
            fav['_deleted'] = True
            # 从有效索引集合中移除
            self._valid_indices.discard(idx)
            break

    # 如果当前分组没有可见卡片了，显示"分组已清空"状态
    if not self._favorite_cards:
        self._show_empty_state(is_group_cleared=True)

    return True
```

**优化后：**
```python
def remove_favorite_card(self, favorite_id: str, deleted_index: int) -> bool:
    """从UI移除卡片（纯UI操作，不处理数据）
    
    Args:
        favorite_id: 收藏ID
        deleted_index: 被删除项的索引（由业务层提供）
        
    Returns:
        bool: 是否成功移除
    """
    card = self._favorite_cards.get(favorite_id)
    if not card:
        return False

    # 只做UI操作
    self.content_layout.removeWidget(card)
    card.deleteLater()
    del self._favorite_cards[favorite_id]

    # 从有效索引集合中移除（业务层已处理数据标记）
    self._valid_indices.discard(deleted_index)

    # 如果当前分组没有可见卡片了，显示"分组已清空"状态
    if not self._favorite_cards:
        self._show_empty_state(is_group_cleared=True)

    return True
```

### 三、调用方修改（ui/palette_management.py）

#### 修改 `_on_favorite_deleted` 方法

**当前代码（第1595-1617行）：**
```python
def _on_favorite_deleted(self, favorite_id):
    """收藏删除回调（软删除）"""
    favorite = self._config_manager.get_favorite(favorite_id, include_deleted=True)
    palette_name = favorite.get('name', tr('palette_management.unnamed')) if favorite else tr('palette_management.unnamed')

    dialog = DeleteConfirmDialog(palette_name, self)

    if dialog.exec():
        # 标记删除
        self._config_manager.delete_favorite(favorite_id)
        self._config_manager.save()

        # 局部刷新：仅移除对应卡片，不重新加载
        success = self.palette_management_list.remove_favorite_card(favorite_id)
        ...
```

**优化后：**
```python
def _on_favorite_deleted(self, favorite_id):
    """收藏删除回调（软删除）"""
    favorite = self._config_manager.get_favorite(favorite_id, include_deleted=True)
    palette_name = favorite.get('name', tr('palette_management.unnamed')) if favorite else tr('palette_management.unnamed')

    dialog = DeleteConfirmDialog(palette_name, self)

    if dialog.exec():
        # 获取删除前的索引（用于UI层更新）
        deleted_index = self._config_manager.get_favorite_index(favorite_id)
        
        # 标记删除
        self._config_manager.delete_favorite(favorite_id)
        self._config_manager.save()

        # 局部刷新：仅移除对应卡片，不重新加载
        success = self.palette_management_list.remove_favorite_card(favorite_id, deleted_index)
        ...
```

### 四、批量删除优化

#### 修改 `delete_selected` 方法

**当前代码（第1227-1239行）：**
```python
def delete_selected(self) -> int:
    """批量删除选中的收藏

    Returns:
        int: 删除的数量
    """
    deleted_count = 0
    for favorite_id in list(self._selected_ids):
        if self.remove_favorite_card(favorite_id):
            deleted_count += 1
    self._selected_ids.clear()
    self.selection_changed.emit(set())
    return deleted_count
```

**优化后：**
```python
def delete_selected(self, deleted_indices: Dict[str, int]) -> int:
    """批量删除选中的收藏

    Args:
        deleted_indices: ID到索引的映射（由业务层提供）
        
    Returns:
        int: 删除的数量
    """
    deleted_count = 0
    for favorite_id in list(self._selected_ids):
        index = deleted_indices.get(favorite_id, -1)
        if index >= 0 and self.remove_favorite_card(favorite_id, index):
            deleted_count += 1
    self._selected_ids.clear()
    self.selection_changed.emit(set())
    return deleted_count
```

#### 修改 `_on_delete_selected_clicked` 方法

**当前代码（第1471-1511行）：**
```python
def _on_delete_selected_clicked(self):
    """删除选中按钮点击"""
    selected_ids = self.palette_management_list._selected_ids
    ...
    if dialog.exec():
        # 从配置中删除
        for favorite_id in selected_ids:
            self._config_manager.delete_favorite(favorite_id)
        self._config_manager.save()

        # 从列表中删除
        deleted_count = self.palette_management_list.delete_selected()
        ...
```

**优化后：**
```python
def _on_delete_selected_clicked(self):
    """删除选中按钮点击"""
    selected_ids = self.palette_management_list._selected_ids
    ...
    if dialog.exec():
        # 获取删除前的索引映射
        deleted_indices = {}
        for favorite_id in selected_ids:
            idx = self._config_manager.get_favorite_index(favorite_id)
            if idx >= 0:
                deleted_indices[favorite_id] = idx
        
        # 从配置中删除
        for favorite_id in selected_ids:
            self._config_manager.delete_favorite(favorite_id)
        self._config_manager.save()

        # 从列表中删除
        deleted_count = self.palette_management_list.delete_selected(deleted_indices)
        ...
```

## 修改清单

| 文件 | 修改类型 | 说明 |
|-----|---------|------|
| `core/config.py` | 新增 | 添加 `get_valid_indices()` 和 `get_favorite_index()` 方法 |
| `ui/palette_management.py` | 修改 | `PaletteManagementList.set_favorites()` 使用业务层接口 |
| `ui/palette_management.py` | 修改 | `PaletteManagementList.remove_favorite_card()` 简化，接收索引参数 |
| `ui/palette_management.py` | 修改 | `PaletteManagementList.delete_selected()` 接收索引映射参数 |
| `ui/palette_management.py` | 修改 | `PaletteManagementInterface._on_favorite_deleted()` 获取索引后传递 |
| `ui/palette_management.py` | 修改 | `PaletteManagementInterface._on_delete_selected_clicked()` 获取索引映射后传递 |

## 职责划分

| 层级 | 职责 |
|-----|------|
| **业务层 (ConfigManager)** | 数据标记、有效索引计算、索引查询、物理清理 |
| **UI层 (PaletteManagementList)** | UI更新、事件触发、接收业务层数据做展示 |

## 优势

1. **职责清晰**：UI层不再直接操作数据，只负责展示
2. **消除重复**：`_deleted` 标记只在业务层设置一次
3. **易于维护**：数据逻辑集中在业务层，便于后续扩展
4. **无过度设计**：保持简单，不引入不必要的抽象层
